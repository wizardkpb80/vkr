#Кофигурация-Web-сервисы запрос
Функция ПрогнозированиеИИ(ДатаС, ДатаПо, НомерЗапроса)

	ЗапросВыполнения = Новый Запрос;
	ЗапросВыполнения.Текст = 
	"// ---- Запрос №1 (Заказы покупателей) ----
	|SELECT
	|	з.Ссылка, з.Дата, з.ВалютаДокумента, з.Контрагент, з.Курс, з.Проект, з.СостояниеЗаказа,
	|	ISNULL(кд.Значение, """") КАК ВидПродукции,
	|	зз.Номенклатура, зз.ЕдиницаИзмерения, зз.Количество, зз.Цена, зз.Объем, зз.ОперацияНТК,
	|	ISNULL(нд.Значение, """") КАК ВысотаПродукции,
	|	ISNULL(ндд.Значение, """") КАК ДлинаПродукции,
	|	ISNULL(ндш.Значение, """") КАК ШиринаПродукции,
	|	сс.Номенклатура КАК Материал, сс.ppfДлина КАК ДлинаРеальная, сс.ppfШирина КАК ШиринаРеальная,
	|	о.cnt КАК КоличествоОпераций, с.cnt КАК КоличествоСпецификаций
	|FROM
	|	Документ.ЗаказПокупателя КАК з
	|	LEFT JOIN Документ.ЗаказПокупателя.ДополнительныеРеквизиты КАК зд
	|		ON з.Ссылка = зд.Ссылка И зд.Свойство.Наименование LIKE ""%Вид продукции%""
	|	JOIN Документ.ЗаказПокупателя.Запасы КАК зз
	|		ON з.Ссылка = зз.Ссылка
	|	JOIN Справочник.Контрагенты КАК к
	|		ON к.Ссылка = з.Контрагент
	|	JOIN Справочник.Номенклатура КАК н
	|		ON н.Ссылка = зз.Номенклатура
	|	LEFT JOIN Справочник.Номенклатура.ДополнительныеРеквизиты КАК нд
	|		ON н.Ссылка = нд.Ссылка И нд.Свойство.Наименование LIKE ""%Высота готовой продукции%""
	|	LEFT JOIN Справочник.Номенклатура.ДополнительныеРеквизиты КАК ндд
	|		ON н.Ссылка = ндд.Ссылка И ндд.Свойство.Наименование LIKE ""%Длина%""
	|	LEFT JOIN Справочник.Номенклатура.ДополнительныеРеквизиты КАК ндш
	|		ON н.Ссылка = ндш.Ссылка И ндш.Свойство.Наименование LIKE ""%Ширина%""
	|	LEFT JOIN Справочник.Контрагенты.ДополнительныеРеквизиты КАК кд
	|		ON кд.Ссылка = к.Ссылка И кд.Свойство.Наименование LIKE ""%Вид продукции%""
	|	LEFT JOIN (SELECT с.Ссылка, с.Владелец, COUNT(*) cnt FROM Справочник.Спецификации КАК с GROUP BY с.Ссылка, с.Владелец) КАК с
	|		ON с.Владелец = н.Ссылка
	|	LEFT JOIN Справочник.Спецификации.Состав КАК сс
	|		ON с.Ссылка = сс.Ссылка
	|		AND сс.ТипСтрокиСостава = ЗНАЧЕНИЕ(Перечисление.ТипыСтрокСоставаСпецификации.Материал)
	|		AND (сс.Номенклатура.Наименование LIKE ""%Латунь%"" OR сс.Номенклатура.Наименование LIKE ""%Алюминий%"" OR сс.Номенклатура.Наименование LIKE ""%Стеклотекстолит%"")
	|	LEFT JOIN (SELECT Ссылка, COUNT(*) cnt FROM Справочник.Спецификации.Операции GROUP BY Ссылка) КАК о
	|		ON о.Ссылка = с.Ссылка
	|WHERE
	|	з.Проведен = ИСТИНА
	|	И з.Дата МЕЖДУ &ДатаС И &ДатаПо
	|	И &НомерЗапроса = 1
	|
	|UNION ALL
	|
	|// ---- Запрос №2 (Заказы на производство) ----
	|SELECT
	|	з.Ссылка, з.Дата, з.ВидОперации, з.ЗаказПокупателя, з.СостояниеЗаказа, ззс.cnt,
	|	зз.Номенклатура, зз.Количество,
	|	рп.РесурсПредприятия, рп.Мощность КАК Тип, рп.Старт, рп.Финиш,
	|	ISNULL(зд.Значение, """") КАК ВремяArtCAM,
	|	ISNULL(зд1.Значение, """") КАК Корректировка,
	|	ISNULL(зд2.Значение, """") КАК Времяработы,
	|	ISNULL(зд3.Значение, """") КАК Брак,
	|	ISNULL(зд4.Значение, """") КАК Пробноеклише,
	|	ISNULL(зд5.Значение, """") КАК ПутьКфайлам,
	|	ISNULL(зд6.Значение, """") КАК Необходимытесты,
	|	ISNULL(зд7.Значение, """") КАК ТестыВыполнены
	|FROM
	|	Документ.ЗаказНаПроизводство КАК з
	|	JOIN Документ.ЗаказНаПроизводство.РесурсыПредприятия КАК рп
	|		ON з.Ссылка = рп.Ссылка
	|	JOIN Документ.ЗаказНаПроизводство.Продукция КАК зз
	|		ON з.Ссылка = зз.Ссылка
	|	JOIN (SELECT Ссылка, COUNT(*) cnt FROM Документ.ЗаказНаПроизводство.Продукция GROUP BY Ссылка) КАК ззс
	|		ON з.Ссылка = ззс.Ссылка
	|	LEFT JOIN Документ.ЗаказНаПроизводство.ДополнительныеРеквизиты КАК зд
	|		ON з.Ссылка = зд.Ссылка И зд.Свойство.Наименование LIKE ""%Время ArtCAM%""
	|	LEFT JOIN Документ.ЗаказНаПроизводство.ДополнительныеРеквизиты КАК зд1
	|		ON з.Ссылка = зд1.Ссылка И зд1.Свойство.Наименование LIKE ""%Корректировка%""
	|	LEFT JOIN Документ.ЗаказНаПроизводство.ДополнительныеРеквизиты КАК зд2
	|		ON з.Ссылка = зд2.Ссылка И зд2.Свойство.Наименование LIKE ""%Время работы%""
	|	LEFT JOIN Документ.ЗаказНаПроизводство.ДополнительныеРеквизиты КАК зд3
	|		ON з.Ссылка = зд3.Ссылка И зд3.Свойство.Наименование LIKE ""%Брак%""
	|	LEFT JOIN Документ.ЗаказНаПроизводство.ДополнительныеРеквизиты КАК зд4
	|		ON з.Ссылка = зд4.Ссылка И зд4.Свойство.Наименование LIKE ""%Пробное клише%""
	|	LEFT JOIN Документ.ЗаказНаПроизводство.ДополнительныеРеквизиты КАК зд5
	|		ON з.Ссылка = зд5.Ссылка И зд5.Свойство.Наименование LIKE ""%Путь к файлам%""
	|	LEFT JOIN Документ.ЗаказНаПроизводство.ДополнительныеРеквизиты КАК зд6
	|		ON з.Ссылка = зд6.Ссылка И зд6.Свойство.Наименование LIKE ""%Необходимы тесты%""
	|	LEFT JOIN Документ.ЗаказНаПроизводство.ДополнительныеРеквизиты КАК зд7
	|		ON з.Ссылка = зд7.Ссылка И зд7.Свойство.Наименование LIKE ""%Тесты выполнены%""
	|WHERE
	|	з.Проведен = ИСТИНА
	|	И з.ЗаказПокупателя.Дата МЕЖДУ &ДатаС И &ДатаПо
	|	И &НомерЗапроса = 2
	|
	|UNION ALL
	|
	|// ---- Запрос №3 (Операции заказов на производство) ----
	|SELECT
	|	з.Ссылка, о.Операция, о.Нормочасы, о.ppfФрезаЦанга,
	|	NULL КАК Дата, NULL КАК ВалютаДокумента, NULL КАК Контрагент,
	|	NULL КАК Курс, NULL КАК Проект, NULL КАК СостояниеЗаказа, NULL КАК ВидПродукции,
	|	NULL КАК Номенклатура, NULL КАК ЕдиницаИзмерения, NULL КАК Количество, NULL КАК Цена,
	|	NULL КАК Объем, NULL КАК ОперацияНТК, NULL КАК ВысотаПродукции, NULL КАК ДлинаПродукции,
	|	NULL КАК ШиринаПродукции, NULL КАК Материал, NULL КАК ДлинаРеальная, NULL КАК ШиринаРеальная,
	|	NULL КАК КоличествоОпераций, NULL КАК КоличествоСпецификаций,
	|	NULL КАК ВидОперации, NULL КАК ЗаказПокупателя, NULL КАК cnt,
	|	NULL КАК РесурсПредприятия, NULL КАК Тип, NULL КАК Старт, NULL КАК Финиш,
	|	NULL КАК ВремяArtCAM, NULL КАК Корректировка, NULL КАК Времяработы, NULL КАК Брак,
	|	NULL КАК Пробноеклише, NULL КАК ПутьКфайлам, NULL КАК Необходимытесты, NULL КАК ТестыВыполнены
	|FROM
	|	Документ.ЗаказНаПроизводство КАК з
	|	JOIN Документ.ЗаказНаПроизводство.Операции КАК о
	|		ON з.Ссылка = о.Ссылка
	|WHERE
	|	з.Проведен = ИСТИНА
	|	И з.ЗаказПокупателя.Дата МЕЖДУ &ДатаС И &ДатаПо
	|	И &НомерЗапроса = 3";

	ЗапросВыполнения.УстановитьПараметр("ДатаС", ДатаС);
	ЗапросВыполнения.УстановитьПараметр("ДатаПо", ДатаПо);
	ЗапросВыполнения.УстановитьПараметр("НомерЗапроса", НомерЗапроса);

	Выборка = ЗапросВыполнения.Выполнить().Выбрать();

	Если Выборка.Количество() = 0 Тогда
		ТипДокумента = ФабрикаXDTO.Тип("http://www.1c.ru/1cwai/AIClishe", "ObjectItem");
		ПакетДокумента = ФабрикаXDTO.Создать(ТипДокумента);
		ТипТабличнойЧасти = ФабрикаXDTO.Тип("http://www.1c.ru/1cwai/AIClishe", "Item");
		ПакетДокументаТаб = ФабрикаXDTO.Создать(ТипТабличнойЧасти);
		ПакетДокументаТаб.ItemMatrixName = "Все заказы выполнены!";
		ПакетДокумента.Item.Добавить(ПакетДокументаТаб);
		Возврат ПакетДокумента;
	КонецЕсли;

	ТипДокумента = ФабрикаXDTO.Тип("http://www.1c.ru/1cwsppf/FactoryClishe", "ObjectItem");
	ПакетДокумента = ФабрикаXDTO.Создать(ТипДокумента);
	ТипТабличнойЧасти = ФабрикаXDTO.Тип("http://www.1c.ru/1cwsppf/FactoryClishe", "Item");

	Если НомерЗапроса = 1 Тогда
		Пока Выборка.Следующий() Цикл
			ПакетДокументаТаб = ФабрикаXDTO.Создать(ТипТабличнойЧасти);
			
			ПакетДокументаТаб.Date = Формат(Выборка.Дата, "ДФ=dd.MM.yyyy ЧЧ:мм");
			ПакетДокументаТаб.OrderNo = Строка(Выборка.Ссылка.Номер);
			ПакетДокументаТаб.CustomerName = СокрЛП(Выборка.Контрагент.Наименование);
			ПакетДокументаТаб.ItemName = СокрЛП(Выборка.Номенклатура.Наименование);
			ПакетДокументаТаб.Qty = Строка(Выборка.Количество);
			ПакетДокументаТаб.Status = СокрЛП(Выборка.СостояниеЗаказа);
			ПакетДокументаТаб.Currency = СокрЛП(Выборка.ВалютаДокумента);
			ПакетДокументаТаб.CurrencyRate = Выборка.Курс;
			ПакетДокументаТаб.Project = СокрЛП(Выборка.Проект);
			ПакетДокументаТаб.ProductType = СокрЛП(Выборка.ВидПродукции);
			ПакетДокументаТаб.Unit = СокрЛП(Выборка.ЕдиницаИзмерения);
			ПакетДокументаТаб.Price = Выборка.Цена;
			ПакетДокументаТаб.Capacity = Выборка.Объем;
			ПакетДокументаТаб.NTKOperation = СокрЛП(Выборка.ОперацияНТК);
			ПакетДокументаТаб.ProductHeight = СокрЛП(Выборка.ВысотаПродукции);
			ПакетДокументаТаб.ProductLength = СокрЛП(Выборка.ДлинаПродукции);
			ПакетДокументаТаб.ProductWidth = СокрЛП(Выборка.ШиринаПродукции);
			ПакетДокументаТаб.Material = СокрЛП(Выборка.Материал);
			ПакетДокументаТаб.ActualLength = Выборка.ДлинаРеальная;
			ПакетДокументаТаб.ActualWidth = Выборка.ШиринаРеальная;
			ПакетДокументаТаб.NumberofOperations = Выборка.КоличествоОпераций;
			ПакетДокументаТаб.NumberofSpecifications = Выборка.КоличествоСпецификаций;
			
			ПакетДокумента.Item.Добавить(ПакетДокументаТаб);
		КонецЦикла;
		
	ИначеЕсли НомерЗапроса = 2 Тогда
		Пока Выборка.Следующий() Цикл
			ПакетДокументаТаб = ФабрикаXDTO.Создать(ТипТабличнойЧасти);
			
			ПакетДокументаТаб.Date = Формат(Выборка.Дата, "ДФ=dd.MM.yyyy ЧЧ:мм");
			ПакетДокументаТаб.OrderNo = Строка(Выборка.Ссылка.Номер);
			ПакетДокументаТаб.CustomerName = СокрЛП(Выборка.РесурсПредприятия);
			ПакетДокументаТаб.ItemName = СокрЛП(Выборка.Номенклатура.Наименование);
			ПакетДокументаТаб.Qty = Строка(Выборка.Количество);
			ПакетДокументаТаб.Status = СокрЛП(Выборка.СостояниеЗаказа);
			ПакетДокументаТаб.Picture = СокрЛП(Выборка.ПутьКфайлам);
			ПакетДокументаТаб.OperationType = СокрЛП(Выборка.ВидОперации);
			ПакетДокументаТаб.OrderBuy = Строка(Выборка.ЗаказПокупателя);
			ПакетДокументаТаб.Quantity = Выборка.cnt;
			ПакетДокументаТаб.PowerType = СокрЛП(Выборка.Тип);
			ПакетДокументаТаб.StartTime = Выборка.Старт;
			ПакетДокументаТаб.EndTime = Выборка.Финиш;
			ПакетДокументаТаб.ArtCAMTime = СокрЛП(Выборка.ВремяArtCAM);
			ПакетДокументаТаб.Adjustment = СокрЛП(Выборка.Корректировка);
			ПакетДокументаТаб.WorkingTime = СокрЛП(Выборка.Времяработы);
			ПакетДокументаТаб.Manufacturing = СокрЛП(Выборка.Брак);
			ПакетДокументаТаб.TestPlate = СокрЛП(Выборка.Пробноеклише);
			ПакетДокументаТаб.RequiredTests = СокрЛП(Выборка.Необходимытесты);
			ПакетДокументаТаб.TestsCompleted = СокрЛП(Выборка.ТестыВыполнены);
			
			ПакетДокумента.Item.Добавить(ПакетДокументаТаб);
		КонецЦикла;
		
	ИначеЕсли НомерЗапроса = 3 Тогда
		Пока Выборка.Следующий() Цикл
			ПакетДокументаТаб = ФабрикаXDTO.Создать(ТипТабличнойЧасти);
			
			ПакетДокументаТаб.OrderNo = Строка(Выборка.Ссылка.Номер);
			ПакетДокументаТаб.ItemName = СокрЛП(Выборка.Операция);
			ПакетДокументаТаб.Qty = Строка(Выборка.Нормочасы);
			ПакетДокументаТаб.ClientOrderNo = СокрЛП(Выборка.ppfФрезаЦанга);
			ПакетДокументаТаб.OrderNoLink = Строка(Выборка.Ссылка);
			
			ПакетДокумента.Item.Добавить(ПакетДокументаТаб);
		КонецЦикла;
	КонецЕсли;
	
	Возврат ПакетДокумента;
	
КонецФункции

// Функция для получения файлов изображений, привязанных к заказам покупателей.
Функция ПолучитьФайлыИзображений(ДатаС, ДатаПо, ФайлКартинки = Неопределено)

	Запрос = Новый Запрос;
	Запрос.Текст = 
	"ВЫБРАТЬ
	|	зк.Ссылка КАК СсылкаЗаказа,
	|	зк.Номер КАК НомерЗаказа,
	|	зк.Дата КАК ДатаЗаказа,
	|	пф.ПутьКФайлу,
	|	пф.Наименование,
	|	пф.Расширение
	|ИЗ
	|	Документ.ЗаказПокупателя КАК зк
	|	ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.ЗаказПокупателяПрисоединенныеФайлы КАК пф
	|		ПО пф.ВладелецФайла = зк.Ссылка
	|		И (пф.Ссылка = зк.ФайлКартинки ИЛИ зк.ФайлКартинки = &ФайлКартинки)
	|		И пф.Расширение В (&Расширения)
	|ГДЕ
	|	зк.Дата МЕЖДУ &ДатаС И &ДатаПо
	|	И зк.Проведен = ИСТИНА";

	Запрос.УстановитьПараметр("ДатаС", ДатаС);
	Запрос.УстановитьПараметр("ДатаПо", ДатаПо);

	// Список разрешённых расширений
	Расширения = Новый Массив;
	Для каждого Элемент Из СтрРазделить("jpeg,jpg,bmp,gif,png,tif", ",") Цикл
		Расширения.Добавить(Элемент);
	КонецЦикла;
	Запрос.УстановитьПараметр("Расширения", Расширения);

	Если ФайлКартинки = Неопределено Тогда
		Запрос.УстановитьПараметр("ФайлКартинки", NULL);
	Иначе
		Запрос.УстановитьПараметр("ФайлКартинки", ФайлКартинки);
	КонецЕсли;

	Выборка = Запрос.Выполнить().Выбрать();

	// Если данных нет – возвращаем информационное сообщение
	Если Выборка.Количество() = 0 Тогда
		ТипДокумента = ФабрикаXDTO.Тип("http://www.1c.ru/1cwai/AIClishe", "ObjectItem");
		ПакетДокумента = ФабрикаXDTO.Создать(ТипДокумента);
		ТипТабличнойЧасти = ФабрикаXDTO.Тип("http://www.1c.ru/1cwai/AIClishe", "Item");
		ПакетДокументаТаб = ФабрикаXDTO.Создать(ТипТабличнойЧасти);
		ПакетДокументаТаб.ItemMatrixName = "Нет файлов изображений за выбранный период";
		ПакетДокумента.Item.Добавить(ПакетДокументаТаб);
		Возврат ПакетДокумента;
	КонецЕсли;

	// Основной пакет
	ТипДокумента = ФабрикаXDTO.Тип("http://www.1c.ru/1cwsppf/FactoryClishe", "ObjectItem");
	ПакетДокумента = ФабрикаXDTO.Создать(ТипДокумента);
	ТипТабличнойЧасти = ФабрикаXDTO.Тип("http://www.1c.ru/1cwsppf/FactoryClishe", "Item");

	Пока Выборка.Следующий() Цикл
		ПакетДокументаТаб = ФабрикаXDTO.Создать(ТипТабличнойЧасти);

		// Заполняем поля по аналогии с ПрогнозированиеИИ
		ПакетДокументаТаб.OrderNo = Строка(Выборка.НомерЗаказа);
		ПакетДокументаТаб.Date = Формат(Выборка.ДатаЗаказа, "ДФ=dd.MM.yyyy ЧЧ:мм");
		ПакетДокументаТаб.ItemName = СокрЛП(Выборка.Наименование);
		ПакетДокументаТаб.Picture = СокрЛП(Выборка.ПутьКФайлу);
		ПакетДокументаТаб.ClientOrderNo = СокрЛП(Выборка.Расширение);
		// Дополнительно можно сохранить ссылку на заказ
		ПакетДокументаТаб.OrderNoLink = Строка(Выборка.СсылкаЗаказа);

		// Если нужны другие поля (например, CustomerName, Qty), их можно добавить,
		// но в исходном запросе они не выбирались.

		ПакетДокумента.Item.Добавить(ПакетДокументаТаб);
	КонецЦикла;

	Возврат ПакетДокумента;

КонецФункции
